OLCS-001  ADT  Selfserve  OLCS Critical Path Integration Test  As a product owner I want to complete a critical path journey on the current Selfserve build So that I can check it works correctly  startSelfserve  startSelfserve,clickApplyForNewLicence,waitGreatBritain,clickGreatBritain
OLCS-002  ADT  Internal  OLCS Critical Path Integration Test  As a product owner I want to complete a critical path journey on the current Internal build So that I can check it works correctly  startInternal  startInternal